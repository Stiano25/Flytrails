import { localImages } from './localImages.js';

/** Page-level heroes — files in `client/public/images/` */
export const pageHeroImages = {
  /** /trips — wide savanna */
  trips: localImages.savannah,
  /** /upcoming-trips — peaks / adventure */
  upcoming: localImages.mountainTop,
  /** Home — final CTA band background */
  homeCta: localImages.mountainTop,
};
